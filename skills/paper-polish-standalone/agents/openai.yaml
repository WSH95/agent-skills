interface:
  display_name: "Paper Polish (Standalone)"
  short_description: "Polish a paper draft into a before/after report"
  default_prompt: "Use $paper-polish-standalone to polish this paper draft into a before/after revision report."
policy:
  allow_implicit_invocation: false
